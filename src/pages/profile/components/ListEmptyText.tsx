import {FC} from "react";
import {ListEmptyTextStyle} from "../styles";

const ListEmptyText:FC = () => {
    return (
        <ListEmptyTextStyle>
                아직 아무것도 없습니다 :)
        </ListEmptyTextStyle>
    );
};

export default ListEmptyText;
